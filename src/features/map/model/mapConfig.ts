import type { Coordinates } from '@/shared/location/model/types';

import type { MapZoomLevel } from './types';

export const DEFAULT_CENTER: Coordinates = { lat: 37.56671, lng: 126.97851 };
export const DEFAULT_LEVEL: MapZoomLevel = 4;
