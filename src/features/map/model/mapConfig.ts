import type { Coordinates } from '@/shared/location/model/types';

import type { MapZoomLevel } from './types';

/**
 * @description 제주 도청 좌표
 */
export const DEFAULT_CENTER: Coordinates = {
	lat: 33.488898,
	lng: 126.49809,
};

export const DEFAULT_LEVEL: MapZoomLevel = 9;
